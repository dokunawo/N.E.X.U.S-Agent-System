# ROADMAP 8 — 2026-06-19 08:28:59

## Goal
Refine the Round Table into a cleaner R.A.M.B.O. home screen with a landscape control-room layout, avatar-based agent identity, globe-linked node bubbles, and a handoff trail that stays easy to carry between AI sessions.

## What Changed

- Shifted the home page into a true three-column landscape layout.
- Moved Council data to the left of the agent roster.
- Kept `R.A.M.B.O.` as the only top-level home title.
- Added clickable globe nodes with hover/click bubbles for each specialist agent.
- Added avatar-style identity visuals for agents and system pages.
- Removed the extra `THE ROUND TABLE` wording from the sidebar brand.
- Refreshed the migration file so the next handoff matches the current build.

## Details

- The home globe now shows linked agent nodes spread around the center with visible connector lines.
- Each node surfaces a short profile bubble with the agent's current work.
- Agent cards and page headers now use tailored avatar art instead of plain letter badges.
- Individual pages now have richer orbital panels instead of feeling visually empty.
- The Round Table remains available as its own page, separate from the R.A.M.B.O. home.

## Next Recommended Steps

1. Turn journal entries into live SQLite-backed records instead of baked sample data.
2. Connect the R.A.M.B.O. home counters to actual task and approval records.
3. Persist Steward planner entries in the backend so the manual budget data survives refreshes.
4. Add export support for the Steward planner if a CSV or spreadsheet handoff is needed later.
5. Move one integration at a time only after Sentinel approves the scope.

## Verification Notes

- Rebuild the Round Table pages after every edit to `round-table/build_pages.py`.
- Check the home page at `http://127.0.0.1:8004/council/`.
- Check the dedicated Round Table page at `http://127.0.0.1:8004/council/round-table.html`.
- Check the Steward download page at `http://127.0.0.1:8004/council/downloads/steward-budget-planner.html`.

