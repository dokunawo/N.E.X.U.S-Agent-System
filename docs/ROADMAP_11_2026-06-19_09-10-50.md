# ROADMAP 11 — 2026-06-19 09:10:50

## Goal
Finalize the approved council layout pass by restoring N.E.X.U.S as a standalone navigation item, removing the duplicate Overseer entry, and keeping the 3D agent node scene as the baseline for individual agent pages.

## What Changed

- Restored `N.E.X.U.S` as its own standalone sidebar item.
- Removed the duplicate `N.E.X.U.S` entry from the old `OVERSEER` section.
- Removed the `OVERSEER` navigation group entirely.
- Kept `Round Table` as the only item inside the `COUNCIL` group.
- Preserved the 3D globe node scene on agent pages.
- Increased the agent node stage size slightly so it does not feel as compact.

## Details

- The standalone `N.E.X.U.S` item now represents the home/overseer surface.
- Specialist agents remain grouped under `SPECIALISTS`.
- Agent pages keep the avatar centered inside the 3D node, with `Tasks completed`, `Tasks queued`, and `Success rate` orbiting around it.
- This is the approved visual baseline before the next backend memory step.

## Next Recommended Steps

1. Connect council journals to SQLite so agent pages reflect live N.E.X.U.S memory.
2. Persist Steward planner entries to the backend so budget data survives reloads.
3. Replace static sample journal content with real task/log records.
4. Wire home and Round Table counters to live task and approval state.

## Verification Notes

- Rebuilt the static council pages with `.\.venv\Scripts\python.exe round-table\build_pages.py`.
- Confirmed `http://127.0.0.1:8004/council/index.html` responds successfully.
- Confirmed generated nav has `N.E.X.U.S` outside `COUNCIL`, with only `Round Table` inside `COUNCIL`.
- Confirmed agent pages include `agent-node-stage` and `core-canvas`.

