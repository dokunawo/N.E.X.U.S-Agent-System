# ROADMAP 9 — 2026-06-19 08:42:37

## Goal
Make the R.A.M.B.O. council feel like a full-screen command center with a cleaner home surface, a richer Round Table orbit, and agent pages that feel like their own living environments.

## What Changed

- Removed the extra roster block from the R.A.M.B.O. home page.
- Added a direct `Open Round Table` call-to-action on the home page.
- Expanded the dashboard width so the site fills the available monitor space instead of leaving a dead right column.
- Reworked the Round Table node ring so agents orbit the globe instead of collapsing into the center.
- Added a dedicated floating avatar scene on each agent page.
- Kept the 3D globe running on every page that uses the shared core canvas.

## Details

- Home now focuses on Council data plus the central globe.
- Round Table now reads as a real orbiting council instead of a stack of overlapping labels.
- Agent pages now have a central avatar scene and floating status bubbles so each page feels inhabited.
- The shared avatar style still needs visual tuning if you want it to match your reference even more closely.

## Next Recommended Steps

1. Tune the avatar artwork closer to the exact style in your reference image.
2. Give the agent orbit a little more motion polish and spacing if you want a more cinematic feel.
3. Move the current mock journal and queue data into SQLite so the pages stop being seeded samples.
4. Connect the home CTA and Round Table data to live dashboard state when the backend is ready.

## Verification Notes

- Rebuild with `.\.venv\Scripts\python.exe round-table\build_pages.py`.
- Verify `http://127.0.0.1:8004/council/`.
- Verify `http://127.0.0.1:8004/council/round-table.html`.
- Verify one agent page, such as `http://127.0.0.1:8004/council/agents/architect.html`.

