# ROADMAP 15 - 2026-06-20 05:27:00

## Goal
Give Approvals and Learning their own 3D node scenes and push the R.A.M.B.O. visual system closer to the realistic neural/orb reference style.

## What Changed

- Replaced the old compact orb panel on Approvals and Learning with a dedicated system node scene.
- Added centered system avatars and cleaner HUD-style labels for Approvals and Learning.
- Added orbiting system labels around each system node.
- Loaded Three.js/core3d on the Approvals and Learning pages.
- Upgraded the shared 3D core with:
  - translucent aura shell
  - denser particle field
  - additive glow
  - faint neural filaments
  - softer rotating depth layers

## Details

- Approvals now presents as a Sentinel-owned system node instead of a flat info panel.
- Learning now presents as a memory/learning node with its own status orbit.
- The old center label issue is fixed by moving the page title and subtitle into a controlled label capsule under the avatar.
- The visuals are still browser-native Three.js, not a full cinematic render engine, but the direction now better matches the futuristic interface references.

## Next Recommended Steps

1. Add a richer neural-web background layer behind the main home/Round Table nodes.
2. Add animated node trails between agents during handoff dispatch.
3. Add `POST /api/workflows/dispatch` so the UI can show real agent handoffs.
4. Add screenshot QA for desktop/mobile visual alignment.

## Verification Notes

- Rebuilt generated council pages with `.\.venv\Scripts\python.exe round-table\build_pages.py`.
- Ran `.\.venv\Scripts\python.exe -m compileall app\app round-table\build_pages.py`.
- Verified Approvals page returns `200 OK`.
- Verified Learning page returns `200 OK`.
- Verified updated `core3d.js` returns `200 OK`.
