# ROADMAP 12 - 2026-06-19 09:16:29

## Goal
Move the old Overseer/N.E.X.U.S information into the standalone N.E.X.U.S home page so the command seat carries the same role, data, directives, and journal context below Council Data.

## What Changed

- Added a `N.E.X.U.S COMMAND SEAT` section below the home page Council Data.
- Pulled the content from the existing `nexus` agent record in `round-table/data/agents.json`.
- Included the old Overseer role, description, permission tier, completed tasks, queued tasks, and success rate.
- Added N.E.X.U.S core directives to the home page.
- Added the N.E.X.U.S mission journal with recently completed, in-progress, and queued entries.
- Added responsive styling so the new command-seat section stacks cleanly on smaller screens.

## Details

- The standalone N.E.X.U.S home item now acts as the combined home/overseer surface.
- The old separate Overseer navigation group remains removed.
- The N.E.X.U.S data is not duplicated by hand in the generated HTML; it is produced from the shared agent data source during the page build.

## Next Recommended Steps

1. Connect the council journals to SQLite-backed live memory.
2. Persist Steward budget planner entries to the backend.
3. Wire the home page Council Data and command-seat counters to live task state.
4. Add a stable server start/stop helper that keeps port `8004` alive reliably from the desktop workflow.

## Verification Notes

- Rebuilt the static council pages with `.\.venv\Scripts\python.exe round-table\build_pages.py`.
- Confirmed generated `round-table/index.html` contains `N.E.X.U.S COMMAND SEAT`.
- Confirmed generated `round-table/index.html` contains N.E.X.U.S core directives and mission journal entries.
- Attempted to restart the local server on `8004`; the helper reported ready once, but the process did not stay available long enough for final `curl` verification.
