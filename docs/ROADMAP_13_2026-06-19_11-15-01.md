# ROADMAP 13 - 2026-06-19 11:15:01

## Goal
Make the council journals and Steward planner real SQLite-backed memory instead of static sample-only pages.

## What Changed

- Added `GET /api/council/journals`.
- Added live journal hydration on R.A.M.B.O. home and individual agent pages.
- Added a `steward_budget_state` SQLite table.
- Added `GET /api/steward/budget`.
- Added `PUT /api/steward/budget`.
- Updated Steward's planner so edits save to local SQLite after changes.
- Added visible planner save status: loading, dirty, saving, saved, or error.
- Updated the Steward planner copy to explain that manual entries persist locally now.

## Details

- Agent journal pages still render baked sample entries first so the static files are readable by themselves.
- When served through FastAPI, shared `app.js` replaces those sample entries with live SQLite journal entries from `/api/council/journals`.
- If SQLite has no journal entries yet, the page now shows empty live sections instead of pretending sample logs are real history.
- Steward planner state is saved as one local JSON payload in SQLite for now. This keeps the first persistence layer simple before splitting finance data into richer reporting tables.

## Next Recommended Steps

1. Add real agent handoff/task-loop records so `agent_logs` starts filling the live journals automatically.
2. Add editable fields for savings goals and investments inside the Steward planner, not only add-card buttons.
3. Add a daily R.A.M.B.O. briefing endpoint that reads weather, local time, calendar status, tasks, journals, and finance state.
4. Improve the visual layer toward the more realistic 3D neural/orb interface while keeping current pages readable.

## Verification Notes

- Rebuilt generated council pages with `.\.venv\Scripts\python.exe round-table\build_pages.py`.
- Ran `.\.venv\Scripts\python.exe -m compileall app\app round-table\build_pages.py`.
- Restarted FastAPI on port `8004`.
- Verified `GET /api/council/journals` responds from SQLite.
- Verified `GET /api/steward/budget` responds.
- Verified Steward budget persistence with an isolated smoke-test SQLite database.
- Verified `steward.html` and `budget.js` are served successfully.
