# ROADMAP 3 - 2026-06-19 04-29-06

This is the third execution log for the R.A.M.B.O. project.

One execution log should capture the work completed during that execution. Future-phase planning belongs inside clear headers, not scattered across separate files.

## Execution Summary

Built the first real R.A.M.B.O. operating backbone and fixed the Windows background startup path.

## ROADMAP 3 - Task, Agent, Log, Memory, And Approval Structure

Status:

```text
Implemented.
```

Owner agents:

- R.A.M.B.O.: final operator.
- Keeper: database and memory structure.
- Pilot: task status and follow-up structure.
- Sentinel: approval and risk boundaries.
- Engineer: implementation and verification.
- Echo: plain-English explanation.

Instructions captured:

- Every request should become a task.
- Every agent action should become a log.
- Every useful result should become memory.
- Every sensitive action should create an approval request.
- The app must keep working in mock mode without API keys.
- R.A.M.B.O. must ask before deleting files, spending money, contacting people, sending messages, connecting accounts, exposing private information, or making difficult-to-reverse changes.

Work completed:

- Added the official agent roster as database-backed seed data.
- Added task records with status, priority, risk level, owner agent, summary, and payload.
- Added agent activity logs tied to task records.
- Added structured memory entries separate from raw logs.
- Added approval request records owned by Sentinel.
- Added data source records for manual entry, mock analytics, Calendar, Gmail, Notes, and Tasks.
- Added API endpoints for schema, agents, tasks, logs, memory entries, and approvals.
- Added Sentinel risk detection for sensitive requests.
- Verified a risky Gmail/email request becomes a pending approval instead of taking action.

Completion gate:

```text
Passed.
```

## ROADMAP 4 - First Real Data Sources

Status:

```text
Planned next layer.
```

Owner agents:

- Link: integration design.
- Sentinel: permission and privacy guard.
- Keeper: source records and local storage.
- Analyst: useful data interpretation.
- Echo: clear summaries for Sir.

Instructions captured:

- Connect useful personal context only after the approval layer is visible.
- Start with read-only integrations.
- Keep mock mode available when API keys or account connections are missing.
- Do not connect bank accounts, spending actions, or high-risk accounts yet.
- Treat Calendar, Gmail, Notes, and Tasks as private by default.

Recommended order:

1. Calendar in read-only mode.
2. Gmail in read-only mode.
3. Notes in read-only mode or manual import.
4. Tasks and reminders in read-only mode.
5. Manual text entries.
6. Spoken entries later after the voice layer exists.

## ROADMAP 5 - Smarter R.A.M.B.O. Reasoning

Status:

```text
Planned after first read-only source.
```

Owner agents:

- R.A.M.B.O.: combines final advice.
- Architect: planning and prioritization.
- Analyst: evidence and patterns.
- Keeper: memory retrieval.
- Sentinel: risk review.
- Echo: final user-facing answer.

Instructions captured:

- OpenAI should remain optional and activate only when `OPENAI_API_KEY` is set.
- The app must still run in mock mode without a key.
- Model-backed planning should use stored task history, memory entries, approvals, and real source summaries.
- R.A.M.B.O. should clearly separate facts, assumptions, risks, and recommendations.

Target abilities:

- Explain what needs attention today.
- Compare current priorities against recent history.
- Identify risks and opportunities.
- Recommend next actions.
- Summarize agent work in plain English.

## ROADMAP 6 - Improved Dashboard And Live Agent Activity

Status:

```text
Planned dashboard layer.
```

Owner agents:

- Engineer: dashboard and backend implementation.
- Pilot: task status display.
- Sentinel: approvals and risk display.
- Analyst: metrics and patterns.
- Echo: readable command-center language.

Instructions captured:

- The dashboard should feel like a calm, professional command center.
- The first screen should be dense and operational.
- It should show priorities, system status, agent activity, inbox items, risks, opportunities, errors, next steps, and completed tasks.
- Text must remain readable and must not overlap on desktop or mobile.

Target dashboard sections:

- Active tasks.
- Pending approvals.
- Recent memory.
- Agent activity.
- Data source status.
- Risks.
- Suggestions.
- Completed work.

## ROADMAP 7 - Voice Input, Spoken Responses, And Wake Phrases

Status:

```text
Planned after dashboard and data loop are useful.
```

Owner agents:

- R.A.M.B.O.: spoken command receiver.
- Echo: spoken response style.
- Pilot: command state and follow-up.
- Sentinel: spoken approval boundaries.
- Engineer: voice implementation.

Instructions captured:

- Primary wake phrase: "Rambo, come online."
- Status phrase: "Rambo, status."
- Work mode phrase: "Rambo, lock in."
- Pause phrase: "Rambo, stand by."
- Natural starts like "Rambo..." should be accepted later.
- Spoken approvals should be supported, but sensitive actions still need clear confirmation.

## ROADMAP 8 - Self-Learning And Self-Improvement

Status:

```text
Planned after approvals, memory, and voice are stable.
```

Owner agents:

- R.A.M.B.O.: final recommendation.
- Architect: improvement strategy.
- Keeper: learned preference storage.
- Sentinel: approval and reversal guard.
- Engineer: safe implementation.
- Pilot: follow-up tracking.

Instructions captured:

- R.A.M.B.O. may learn preferences and repeated workflows over time.
- Self-improvement must be logged, explainable, and reversible.
- Approval is required when behavior changes are important, sensitive, broad, or difficult to reverse.
- No hidden automation should be added.

## ROADMAP 9 - Hybrid Cloud And Local Execution

Status:

```text
Longer-term planned layer.
```

Owner agents:

- Architect: deployment strategy.
- Engineer: cloud and local runtime.
- Link: external service connections.
- Keeper: memory sync and storage.
- Sentinel: privacy, secrets, and access boundaries.
- Pilot: uptime and follow-up checks.

Instructions captured:

- Cloud can handle always-on orchestration, scheduled monitoring, API integrations, and memory sync.
- Local execution should handle private files, local scripts, desktop automation, and sensitive data.
- Secrets must stay in `.env` files or approved secret storage.
- Broad system-wide changes require approval.
- The app must keep a local mock mode for safe development.

## Startup Wrapper Fix

Problem:

```text
The earlier background startup attempts got stuck or exited because the managed shell did not detach the server cleanly.
```

Fix completed:

- Added a dedicated background launcher at `scripts/start_rambo_background.py`.
- Updated `Start-RAMBO.ps1` to support `-Background`.
- Updated `Start-Jarvis.ps1` to forward to `Start-RAMBO.ps1`.
- Added runtime PID and log output under `data`.
- Verified the background server stayed available on port `8004`.

## Verification

Checks completed:

- Python compile passed for `app\app` and `scripts`.
- PowerShell syntax check passed for startup scripts.
- `GET /api/health` passed.
- `GET /api/schema` passed.
- Dashboard page loaded at `http://127.0.0.1:8004`.

## Recommended Next Move

Build the approval workflow UI and approval decision endpoints.

Reason:

Before connecting Gmail, Calendar, Notes, or Tasks, Sir needs a visible way to approve or reject pending actions.
