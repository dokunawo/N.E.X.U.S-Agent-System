# ROADMAP 14 - 2026-06-19 13:56:28

## Goal
Translate the useful Devchain-style operating patterns into R.A.M.B.O. without importing a separate platform, then add the first daily briefing surface and scalable agent prompt guide.

## What Changed

- Added `app/app/services/briefing.py`.
- Added `GET /api/briefing/daily`.
- Added a Daily Briefing panel to the R.A.M.B.O. home page.
- Added a Devchain-inspired Operating Loop panel to the R.A.M.B.O. home page.
- Mapped workflow stages into R.A.M.B.O. agents:
  - Capture: R.A.M.B.O., Echo, Pilot
  - Plan: Architect, Pilot
  - Validate: Seeker, Analyst, Sentinel
  - Build: Engineer, Link, Keeper
  - Review: Sentinel, Analyst, Echo
  - Remember: Keeper, Pilot, R.A.M.B.O.
- Added `docs/RAMBO_AGENT_SCALING_PROMPTS.md`.
- Styled the new panels with a more futuristic neural/glass interface direction.

## Details

- Calendar is represented as `not_connected` until Link and Sentinel wire an approved Google Calendar integration.
- Weather uses a no-key Open-Meteo request for Detroit, Michigan, with a safe fallback if the local server cannot reach the network.
- The operating loop is approval-gated: low-risk internal handoffs can continue, but money, messages, deletion, private data, and account connections still stop at Sentinel/Sir approval.

## Next Recommended Steps

1. Add structured handoff/task-loop records so the operating loop can write real logs into SQLite.
2. Add `POST /api/workflows/dispatch` for low-risk agent routing.
3. Add a daily briefing history table so R.A.M.B.O. remembers each briefing it gave.
4. Wire Calendar after Sentinel approval so the briefing can show real meetings.
5. Continue visual polish toward the more realistic 3D neural/orb interface.

## Verification Notes

- Rebuilt generated council pages with `.\.venv\Scripts\python.exe round-table\build_pages.py`.
- Ran `.\.venv\Scripts\python.exe -m compileall app\app round-table\build_pages.py`.
- Restarted FastAPI on port `8004`.
- Verified `GET /api/briefing/daily` returns a complete briefing payload.
- Verified `GET /council/index.html` returns `200 OK`.
- Weather currently returns the fallback status from the local server because the server could not reach Open-Meteo in this environment.
