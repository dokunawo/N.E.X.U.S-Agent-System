# ROADMAP 17 - 2026-06-20 06-19-25

## Execution Goal

Finish the R.A.M.B.O. naming cleanup by replacing Sir's personal name in UI/code/docs and removing the remaining legacy project-name references from text, filenames, and generated pages.

## What Changed

- Updated the daily briefing greeting to say `Good morning, Sir`.
- Replaced Sir's personal name with `Sir` across active UI text, backend strings, docs, and generated pages.
- Removed remaining legacy project-name references from active text/code documents.
- Renamed legacy docs and starter compatibility file names to R.A.M.B.O. naming.
- Switched the internal operator key and generated operator profile page to `rambo`.
- Renamed the local SQLite memory file to `rambo_memory.sqlite3`.
- Migrated existing SQLite operator references from the old internal key to `rambo` so local memory stays usable.
- Rebuilt the Round Table pages and verified the new `rambo.html` route.

## Verification

- `GET /api/briefing/daily` returns `Good morning, Sir. R.A.M.B.O. online. Systems synchronized. Standing by for your command.`
- `GET /council/agents/rambo.html` returns `200 OK`.
- Search found no remaining active personal-name, old project-name, or old tagline references in app/docs/static sources.
- Rebuilt generated pages and compiled backend files successfully.
