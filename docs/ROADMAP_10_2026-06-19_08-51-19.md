# ROADMAP 10 — 2026-06-19 08:51:19

## Goal
Refine the council UI so the N.E.X.U.S home screen reads like one full-width command surface, the Round Table ring is clearly separated, and each agent page presents a single centered 3D node with orbiting task stats.

## What Changed

- Removed the leftover `COUNCIL HOME` banner text from the home page.
- Kept `N.E.X.U.S` visually separate from the council navigation.
- Left only `Round Table` inside the Council section of the sidebar.
- Moved the home globe above the council data block.
- Changed the home CTA to `View Council Members`.
- Removed the extra agent orb panel so each agent page relies on one centered 3D scene.
- Rebuilt the Round Table orbit so the agents sit around the globe instead of crowding the middle.

## Details

- The home page now uses the globe as the lead visual, with council data below it.
- Agent pages now show the avatar/node with `Tasks completed`, `Tasks queued`, and `Success rate` orbiting the center.
- The top-left text on the home page is now cleaner and less repetitive.
- The next system-level improvement is still to replace the sample journals and planner storage with SQLite-backed live memory.

## Next Recommended Steps

1. Wire council journals to SQLite so the pages show live memory instead of baked samples.
2. Persist Steward planner entries to the backend so the planner survives reloads.
3. Keep tightening the avatar art if you want it closer to the reference image.
4. Once the data layer is live, connect the home counters to real task and approval records.

## Verification Notes

- Rebuild with `.\.venv\Scripts\python.exe round-table\build_pages.py`.
- Check the home page at `http://127.0.0.1:8004/council/`.
- Check the Round Table page at `http://127.0.0.1:8004/council/round-table.html`.
- Check one agent page at `http://127.0.0.1:8004/council/agents/architect.html`.

